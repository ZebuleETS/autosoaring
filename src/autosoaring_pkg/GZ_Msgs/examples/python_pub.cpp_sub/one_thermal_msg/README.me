# Thermal Group Message Exchange

This project demonstrates the exchange of `thermal` messages between a Python publisher and a Python subscriber using Gazebo Transport (`gz-transport13`). The publisher sends a `thermal` containing  `Thermal` message, and the subscriber processes and prints the details of the thermal.
