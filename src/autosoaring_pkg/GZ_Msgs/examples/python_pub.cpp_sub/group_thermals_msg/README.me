# Thermal Group Message Exchange

This project demonstrates the exchange of `ThermalGroup` messages between a Python publisher and a Python subscriber using Gazebo Transport (`gz-transport13`). The publisher sends a `ThermalGroup` containing multiple `Thermal` messages, and the subscriber processes and prints the details of each thermal in the order they are received.
